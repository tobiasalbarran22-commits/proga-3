# Copiá este archivo como .env.local (o .env.production) y completá los valores.

# URL del backend FastAPI, sin barra al final.
NEXT_PUBLIC_API_URL=http://localhost:8000

# true solo para trabajar con el backend simulado de app/api/mock.
MOCK_API_HABILITADO=false
